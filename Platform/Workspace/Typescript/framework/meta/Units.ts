import { ids } from '../../meta/generated/ids.g';

export const unitIdByTypeName = {
  Binary: ids.Binary,
  Boolean: ids.Boolean,
  DateTime: ids.DateTime,
  Decimal: ids.Decimal,
  Float: ids.Float,
  Integer: ids.Integer,
  String: ids.String,
  Unique: ids.Unique,
};

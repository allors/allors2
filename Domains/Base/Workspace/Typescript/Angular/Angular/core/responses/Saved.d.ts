import { ISession, PushResponse } from "@allors/framework";
export declare class Saved {
    session: ISession;
    response: PushResponse;
    constructor(session: ISession, response: PushResponse);
}
